- defaults:
    link:
      relink: true

- clean: ['~']

- clean:
    ~/.config:
      force: true

- clean:
    ~/.tmux:
      force: true
      # - clean :
      # ~/.bash_completion:
      # force: true

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.zprofile: zprofile
    ~/.gitconfig: gitconfig
    ~/conda-zsh-completion:
        path: conda-zsh-completion
        force: true
        create: true
    ~/.bash_profile:
        path: bash_profile
        create: true
    ~/.bashrc: bashrc
    ~/.xonsh: xonsh
    ~/.aliases: aliases
    ~/.alacritty.yml:
        path: alacritty.yml
        force: true
    ~/.functions: functions
    ~/.variables: variables
    ~/.taskrc:
        path: taskrc
        glob: true
        force: true
        create: true
    # ~/.task:
    #     path: task/*
    #     glob: true
    #     force: true
    #     create: true
    ~/.fonts:
        path: fonts/*
        glob: true
        force: true
        create: true
    ~/.config:
        path: config/*
        glob: true
        force: true
        create: true
    ~/.tmux:
        path: tmux/*
        glob: true
        force: true
        create: true
    ~/.zsh-completions:
        path: zsh-completions
        force: true
        create: true
    ~/.oh-my-zsh:
        path: oh-my-zsh
        force: true
        create: true
    ~/.zshrc:
        path: zshrc
        force: true
    ~/.bash_completion:
        path: bash_completion
        # glob: true
        create: true
        force: true
    ~/.inputrc:
        path: inputrc
        force: true
        create: true
    ~/.zplug:
        path: zplug
        force: true
        create: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [fc-cache -v -f, Add fonts]
