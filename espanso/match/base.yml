# Automatically generated by espanso migration tool
# Original file: default.yml
ast_inject: false
backend: Clipboard

matches:
  - trigger: ":cfold:"
    replace: |-
      <details><summary><code>Click to expand</code></summary>

      ```python
      ```
      </details>
  - trigger: ":fold:"
    replace: |-
      <details><summary>Click to expand</summary>

      </details>
  - trigger: ":pd:"
    replace: |
      import pandas as pd
  - trigger: ":np:"
    replace: |
      import numpy as np
  - trigger: ":mpl:"
    replace: |
      from matplotlib import pyplot as plt
  - trigger: ":linestyle:"
    replace: linkStyle default interpolate basis
  - trigger: ":h:"
    replace: |
      --help | less
    force_clipboard: true
  - trigger: ":hdate:"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "## %A %Y-%m-%d"
  - trigger: ":ddate:"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%A %Y-%m-%d"
  - trigger: ":date:"
    replace: "{{mydate}}"
    force_clipboard: true
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
  - trigger: ":mail:"
    replace: mmngreco@gmail.com
  - trigger: ":name:"
    replace: Maximiliano Greco
  - trigger: ":sshcom:"
    replace: |
      sshpass -p {{pass}} ssh mgreco@etstea10
      clear
    vars:
      - name: pass
        type: match
        params:
          trigger: ":amf:"
  - trigger: ":labels:"
    replace: |-
      /label ~"assignee:mgreco"
      /label ~"role:it" ~ToDo
      /label ~"2022:objective:tesser" ~"effort:normal" ~"priority:normal" ~"uncertainty:medium"
      /milestone %"sprint:54"
      /assign @mgreco
    force_clipboard: true
  - trigger: ":task:"
    replace: |-
      ## Contexto

      ## Problema

      ## Objetivo

      ## Tasks

      - [ ] implementar tests

      {{labels}}
    force_clipboard: true
    vars:
      - name: labels
        type: match
        params:
          trigger: ":labels:"
