#!/usr/bin/env bash
bash -c "pipx install copier"
bash -c "pipx install cookiecutter"
bash -c "pipx install pydoro"
bash -c "pipx install sqlparse"
bash -c "pipx install visidata"
bash -c "pipx install vdsql"
bash -c "pipx install lookatme"
bash -c "pipx install httpie"
bash -c "pipx install s3cmd"
bash -c "pipx install pdftotext"
bash -c "pipx install trash-cli"
bash -c "pipx install shot-scraper"
bash -c "pipx install shell-gpt"
bash -c "pipx install ruff"
bash -c "pipx install isort"
bash -c "pipx install flake8"
bash -c "pipx install black"
bash -c "pipx install pydocstyle"
