# Automatically generated by espanso migration tool
# Original file: default.yml
matches:
  - trigger: ":gpt:"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "xdotool key ctrl+a && xdotool key ctrl+x && /home/mgreco/.local/bin/pbpaste | sed 's/:gpt://' | espanso_gpt"

  - trigger: ":retro:"
    replace: "1. Max 🔴🟡🟢\n    - 🔁 \n    - ✅ \n🧩🧩 "

  - trigger: ":draft:"
    replace: "---\ntitle: \"$|$\"\ndate: {{date}}\ndraft: false\ncategories: []\nlabels: []\n---\n\n"
    vars:
      - name: date
        type: match
        params:
          trigger: ":date:"

  - trigger: ":today:"
    replace: "/heading 3\n@Today \n"

  - trigger: ":plt:"
    replace: "from matplotlib import pyplot as plt\nplt.show()\n"

  - trigger: ":paste:"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "xdotool key ctrl+v"

  - trigger: ":cut:"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "xdotool key ctrl+x"

  - trigger: ":copy:"
    replace: "{{out}}"
    vars:
      - name: out
        type: shell
        params:
          cmd: "xdotool key ctrl+a && xdotool key ctrl+c"

  - trigger: ":pass:"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "passgen"

  - trigger: ":myip:"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "curl 'https://api.ipify.org'"

  # - trigger: "doops"
  #   replace: "data-observatory-ops"

  - trigger: ":details:"
    replace: |-
      <details><summary>Click to expand</summary>

      </details>

  - trigger: ":fold:"
    replace: |-
      <details><summary>Click to expand</summary>

      </details>

  - trigger: ":import:"
    replace: |
      import pandas as pd
      import numpy as np
      from matplotlib import pyplot as plt

  - trigger: ":linestyle:"
    replace: linkStyle default interpolate basis

  - trigger: ":h2 date:"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "## %A %Y-%m-%d"

  - trigger: ":Date:"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%A %Y-%m-%d"

  - trigger: ":date:"
    replace: "{{mydate}}"
    force_clipboard: true
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"

  - trigger: ":time:"
    replace: "{{time}}"
    vars:
      - name: time
        type: date
        params:
          format: "%H:%M"

  - trigger: ":month:"
    replace: "{{date}}"
    vars:
      - name: date
        type: date
        params:
          format: "%B %Y"

  - trigger: ":mail:"
    replace: mmngreco@gmail.com

  - trigger: ":name:"
    replace: Maximiliano Greco

  - trigger: ":link:"
    replace: "[$|$]({{clipboard}})"
    vars:
    - name: "clipboard"
      type: "clipboard"

  - trigger: ":a:"
    replace: "<a href=\"{{clipboard}}\" />$|$</a>"
    vars:
    - name: "clipboard"
      type: "clipboard"

  - trigger: ":code:"
    replace: "```python\n$|$\n```"
