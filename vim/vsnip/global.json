{
  "Task": {
    "prefix": "task",
    "body": [
        "${VIM:strftime('%Y-%m-%d')}\t$1\t$2\t$2\tmgreco\t$3\n$0"
    ]
  },
  "uuid": {
    "prefix": "uuid",
    "body": [
      "${VIM:system('python -c \"import uuid, sys;sys.stdout.write(str(uuid.uuid4()))\"')}"
    ]
  },
  "date": {
      "prefix": "date",
      "body": ["${VIM:strftime('%Y-%m-%d')}"]
  },
  "time": {
      "prefix": "time",
      "body": ["${VIM:strftime('%H:%M:%S')}"]
  }
}

