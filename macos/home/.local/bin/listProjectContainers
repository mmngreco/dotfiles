#!/usr/bin/env zsh

listGitProjects () {
    # find projects using .git folder as reference
    for val in ${MYGITPROJECTS//:/ }; do
        if [ -d "$val" ]; then
           fd -H -t d -d 4 -g '.git' $val -x dirname {} \; -x dirname {} \; | sort -u
        fi
    done
}

listGitProjects
