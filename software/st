#/usr/bin/env bash

dir=~/st
[ -d $dir ] && rm -rf $dir
git clone --depth 1 https://git.suckless.org/st $dir

pushd $dir > /dev/null

for patch in ~/.dotfiles/st-patches/*.diff; do
    git apply $patch
done

sudo make clean install
popd > /dev/null
